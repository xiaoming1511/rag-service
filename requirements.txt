# 核心依赖
chromadb>=0.4.0          # 向量数据库
openai>=1.0.0            # oMLX 兼容 OpenAI API
pydantic>=2.0.0          # 数据模型
pyyaml>=6.0              # YAML 配置

# 文档处理
markdown>=3.5            # Markdown 解析
beautifulsoup4>=4.12     # HTML 清理/网页解析
pymupdf>=1.24            # PDF 解析（中文支持好）
python-docx>=1.1         # Word (.docx) 解析
ebooklib>=0.18           # EPUB 电子书解析
python-pptx>=0.6.23      # PowerPoint (.pptx) 解析

# API 服务 (后期)
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6   # /v1/import 文件上传需要

# 增量索引文件监听
watchdog>=4.0.0

# 工具
tqdm>=4.66               # 进度条
python-dotenv>=1.0.0     # 环境变量